setwd("/Users/imiu/Programming/R/datasc")
options(prompt="R:datasc> ")
# saveg <- function (fn, width = 3, height = 3, pointsize = 8)
# {
#   dev.copy(device = pdf, file = paste(fn, ".pdf", sep = ""),
#            width = width, height = height,
#            pointsize = pointsize)
#   dev.off()
# }

#options(prompt="R:ch1> ", warn = 2,  error = utils::recover)
library(utils)
library(base)
library(gRbase)
library(graphics)
library(grDevices)
library(ggplot2)

.libPaths() # get library location
library()   # see all packages installed
search()    # see packages currently loaded
getwd()
options(width=200)

cat("\nIonel, welcome back to R! Today is", date(), "\n")
cat("\nR is ready for you!\n")
cat("\nWorking director is: ", getwd(), "\n")
cat("\nPackages currently loaded are:\n", search(), "\n")

cat("\n                    ======================================", "\n")
cat("\n                                 datasc"                   , "\n")
cat("\n                    ======================================", "\n")


